# OCR GUI Tool - 依赖配置文件

# 安装命令:
pip install -r requirements.txt

# GUI 框架
PySide6>=6.5.0           # Qt 框架（必须）
PySide6-Fluent-Widgets   # Fluent Design 界面（必须）

# 可选界面
pywebview>=4.0           # Web UI（可选）

# 数据处理
Pillow>=9.0.0            # 图片处理
openpyxl>=3.0.0          # Excel 读写
pandas>=2.0.0            # 数据清洗 & 透视（必填）

# 工具
pyperclip>=1.8.0         # 剪贴板操作